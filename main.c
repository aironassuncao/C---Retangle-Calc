#include <stdio.h>
#include <stdlib.h>

int main()
{
  double base, altura, diagonal, perimetro, area;

  printf ("base do retangulo: ");
  scanf ("%lf", &base);

  printf ("altura do retangulo: ");
  scanf ("%lf", &altura);


    return 0;
}
